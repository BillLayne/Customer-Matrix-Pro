# September 7, 2026 Release

Approved by Bill: implement the reviewed recommendations and push live. Scope is the agent Agency Command Center, with the staff companion updated only for shared contact/authentication/API compatibility. Both worktrees were clean before work began.

## Changes

- Search-first, separate Search / Tools / Images workspaces. Compact header, phone bottom navigation, readable labels and consistent neutral surfaces with agency blue actions.
- Six search modes accessible on narrow phones. Existing Matrix, Google Drive, carrier, and legacy NC Tools destinations preserved.
- Complete 45-entry launcher inventory retained byte-for-byte. Real anchor links remove false popup-blocked messages; opening tools records recents. Pinned pagination does not discard pins. One slash shortcut focuses Unified Search.
- Command palette includes shared/manual contacts; dialogs retain focus and support Escape. Persistent errors can be dismissed. Workspace changes preserve filter/draft state.
- Audit Memo saves a local draft, preserves it on close/reload, supports Undo after AI rewriting, and does not discard notes on clipboard/AI failure. Explicit cancellation and retry are available. Reports are sanitized before export.
- Library and Upload views, cached full-inventory search, progress/cancel/retry, image dimensions/date information, 25 recents, and upload success separated from clipboard success. Existing BLI Image Host backend is unchanged.
- Signed server-side sessions and private replacement passwords for both dashboards. Fixed approved cookies/client access-code checks removed. APIs require authentication and reject cross-origin writes. No key is injected into browser code.
- Agent/staff share a private D1 contact directory. Revision-checked saves, offline pending queues, conflict review, backup/restore, and 90-day version retention. Twenty existing private saved entries were hash-matched to Bill's browser and seeded outside public source.
- Compatible dependency security updates; no framework migration. Dependency audits return zero known advisories in both repos.

## Verification

- Both repos: TypeScript lint, production build, eight server regression tests, and fifteen contact client checks passed.
- Contact checks include offline recovery, once-only migration, deletion preservation, lost responses, same-ID conflicts, cross-tab edits, backup validation, server limits, and session expiry. No network access in unit tests.
- Authenticated Cloudflare previews: sign-in, signed secure cookies, anonymous/forged-cookie denial, cross-origin rejection, shared contact reads, and key-free bundles passed.
- Real preview D1: agent addition appeared for staff, staff edit appeared for agent, stale save returned 409, and synthetic entry was removed. Production received only the verified private seed, not test contacts.
- Integrated agent browser: Search, Contacts, Tools, and Images fit 320, 390, 768, 1280, and 1440px with no horizontal overflow. Search input stays within the initial viewport. Screenshots inspected, including dark desktop, phone memo, and narrow contact results.
- Command palette keyboard focus, Escape, slash routing, filter retention across workspace changes, and memo persistence after reload passed.
- Scoped browser/service fixtures: full 1,076-image inventory, last-page matching, cache/refresh, image upload success with failed clipboard, AI timeout/cancel/error handling, original-note Undo, report sanitizer, and exact legacy NC handoff passed. Upload traffic was intercepted; no real images were uploaded.
- All 45 `PROGRAMS` definitions exactly match the pre-release source. Dormant Gmail files and static utilities remain untouched.

## Still Unverified

**The saved Gemini API key is invalid.** Server-side AI and recovery are implemented, but successful live memo organization, document extraction, county discovery, and property generation cannot be confirmed until a current key is supplied. Do not mark these as repaired end-to-end. Bill was asked to supply the key privately via clipboard; no replacement was received during implementation.

Physical mobile keyboard behavior, third-party account logins, actual customer sends, and live image uploads were not tested. No customer messages, real forms, or customer records were changed. Contacts are shared agency data; separate per-person accounts/permissions and named audit attribution are not implemented.

## Operational Notes

- Agent production: https://customer-matrix-pro.pages.dev/
- Staff production: https://agency-staff-dashboard.pages.dev/
- Agent preview alias: https://codex-command-center-review.customer-matrix-pro.pages.dev/
- Staff preview alias: https://codex-command-center-review.agency-staff-dashboard.pages.dev/
- Production D1: `agency-shared-contacts`; preview D1: `agency-shared-contacts-preview`. Never point preview test writes at production.
- Login passwords are in `../Agency-Staff-Contact-Backups/command-center-access-2026-09-07.txt`, outside both public repos. One fresh sign-in is required on each dashboard. Share only the staff password with authorized staff.
- Source and deployment procedures: `CUSTOMER_MATRIX_PRO_AI_HANDOFF.md` and staff `README.md`.
- Preview browser screenshots and private test session state are ignored under `output/qa`. Do not commit that directory. Test helpers do not print secrets.
- Deployment order: D1 migration/verified seed, private configuration, approved commits/pushes, then both Pages deployments. Verify actual production bundle filenames and authenticated API responses, not only the Wrangler success message.
